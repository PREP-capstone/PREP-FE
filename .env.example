# FastAPI 백엔드 서버 주소.
# 이 파일을 복사해서 .env.local 로 만드세요. (.env.local은 .gitignore 처리되어 커밋되지 않습니다)
#
# 배포 서버 (기본값, 별도 설정 없어도 client.js에서 이 주소를 씀):
VITE_API_BASE_URL=https://api.prepwell.shop

# 팀원 로컬 PC의 uvicorn 서버를 쓰는 경우 (같은 와이파이 필요):
# VITE_API_BASE_URL=http://172.17.33.52:8000
